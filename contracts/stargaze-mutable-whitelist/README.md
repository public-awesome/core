# Stargaze Mutable Whitelist

This contract is used to manage a whitelist of addresses that can be used to mint tokens among other things.

It can work with bech32 Cosmos addresses or any other address that is in a string format. Bech32 addresses are validated before being added to the whitelist.
